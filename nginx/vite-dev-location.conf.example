# Snippet for nginx — proxy HTTPS site to Vite dev (127.0.0.1:7598) with WebSocket support for HMR.
# Include inside server { listen 443 ssl; server_name zeruxcom-ds.zerux.com; ... }
#
# Required for Vite HMR: Upgrade + Connection headers.

location / {
    proxy_pass http://127.0.0.1:7598;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 86400;
}
